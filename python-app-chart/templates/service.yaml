apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-python-app"
spec:
  type: NodePort
  selector:
    app: python-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}